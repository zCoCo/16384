function Report_2214()
    % Setup Robot:
    dhp = [...
        0,			pi/2,		116.23e-3,		0;		...
        327.76e-3,	pi,			0,				0;		...
        0,          -pi/2,      2.5e-3,			-pi/2;	...
        254.10e-3,	pi/2,		94.05e-3,		-pi/2;	...
        266.70e-3,	pi/2,		54.23e-3,		0		...
    ];
    actuatedJoints = 4 * ones(size(dhp,1),1);
    robot = Robot3D(dhp, actuatedJoints);
    
    q0 = [pi/2; pi/4; -pi/4; pi/2; 0]; % Rough guess in the plane for a starting theta.
    Xh = [-0.056730; 0.525; 0.27];
    qh = robot.ikd(q0, [Xh; 0;0;0], robot.dof, [1,2,3]); % Only 1,2,3 because we have information about / care about an xyz position (no orientation).
    qh = robot.ikd(qh, [Xh; 0;0;0], robot.dof, [1,2,3]) % Run twice to ensure convergence since q0 was a poorly-informed guess.
    
end % #Report_2214